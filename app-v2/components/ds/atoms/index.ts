/**
 * Content atoms — no width or section opinion. Placed into a layout's
 * grid by the page composing them (via `style={{ gridColumn, gridRow }}`
 * etc.). See components/ds/README.md.
 */
export { default as Quote } from "./Quote";
export { default as ImageFrame } from "./ImageFrame";
export { default as MetricStat } from "./MetricStat";
